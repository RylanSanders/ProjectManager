<Window x:Class="ProjectManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ProjectManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:MainWindow.SwitchPageCommand}"
                    Executed="SwitchPageExecuted"/>
    </Window.CommandBindings>
    <Grid>
        <Frame x:Name="ContentFrame" Source="Pages/TimerPage.xaml" NavigationUIVisibility="Hidden"></Frame>
        <md:DrawerHost LeftDrawerBackground="{DynamicResource MaterialDesignPaper}">
            <md:DrawerHost.LeftDrawerContent>
                <StackPanel Width="150">
                    <Button Height="32" Content="Calendar" Command="{x:Static local:MainWindow.SwitchPageCommand}" CommandParameter="Pages/CalendarPage.xaml" />
                    <Button Height="32" Content="Timer" Command="{x:Static local:MainWindow.SwitchPageCommand}" CommandParameter="Pages/TimerPage.xaml" />
                </StackPanel>
            </md:DrawerHost.LeftDrawerContent>

            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                <!-- Binding the Is*DrawerOpen, you can also bind this to a boolean in your view model -->
                <ToggleButton IsChecked="{Binding IsLeftDrawerOpen, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type md:DrawerHost}}}" 
                          Style="{StaticResource MaterialDesignActionLightToggleButton}" ToolTip="Open Left Drawer">
                    <md:PackIcon Kind="Menu" Height="32" Width="Auto" />
                </ToggleButton>
            </StackPanel>

        </md:DrawerHost>
    </Grid>
</Window>
