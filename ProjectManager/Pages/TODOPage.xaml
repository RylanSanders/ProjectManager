<Page x:Class="ProjectManager.Pages.TODOPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectManager.Pages"
      xmlns:converters="clr-namespace:ProjectManager.Converters"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TODOPage">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:ProjectNameConverter x:Key="ProjectNameConverter"/>
            <converters:DateTimeStringConverter x:Key="DateTimeStringConverter"/>
        </ResourceDictionary>
        
    </Page.Resources>

    <Grid Margin="50,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="IntervalListBox" SelectionChanged="IntervalListBox_SelectionChanged" SelectionMode="Single">
        </ListBox>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <TreeView x:Name="TodoTreeView" VirtualizingPanel.IsVirtualizing="True">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding SubTodos}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{Binding IsCompleted}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                            <TextBlock Grid.Column="2" Text="{Binding Description}"/>
                            <TextBlock Grid.Column="3" Text="{Binding EndDate, Converter={StaticResource DateTimeStringConverter}}"/>
                            <Button x:Name="DeleteTODOButton"  Grid.Column="4" Style="{StaticResource RedPurpleHoverButton}"  Height="30" Width="30" Margin="-7" Click="DeleteTODOButton_Click">
                                <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Margin="-10, -11"/>
                            </Button>
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <Button Grid.Row="1" Content="Add TODO" x:Name="AddTODOButton" Style="{StaticResource GreenPurpleHoverButton}" Click="AddTODOButton_Click"/>
        </Grid>
        
    </Grid>
</Page>
